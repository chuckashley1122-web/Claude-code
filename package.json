{
  "name": "ghl-integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GoHighLevel API v2 client and example scripts",
  "scripts": {
    "build": "tsc",
    "build:mcp": "esbuild src/mcp-server.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/ghl-mcp-server.mjs --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx src/scripts/smoke.ts",
    "contacts:demo": "tsx src/scripts/contacts-demo.ts",
    "opportunities:demo": "tsx src/scripts/opportunities-demo.ts",
    "conversations:demo": "tsx src/scripts/conversations-demo.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
