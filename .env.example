# Template only — copy, fill in, and never commit the real values.
# See docs/jarvis-setup.md and docs/ghl-mcp-setup.md.

# ElevenLabs MCP (Jarvis). Export in your shell profile or keychain.
ELEVENLABS_API_KEY=sk_...

# GoHighLevel MCP.
# Token: dashboard -> Settings -> Integrations -> Private Integrations.
# It does not expire on its own — treat it as a password and revoke it if leaked.
GHL_PIT_TOKEN=pit-...
GHL_LOCATION=te1aYxyEeYsXcFJhnET6
